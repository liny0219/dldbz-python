import uiautomator2 as u2
from utils.controller import DeviceController
from utils.move import MoveEngine
from utils.battle import BattleEngine
from utils.comparator import Comparator

if __name__ == '__main__':
    controller = DeviceController()


    import time
    time.sleep(999)
    # comparator = Comparator(controller)

    # mover = MoveEngine(controller)
    # battler = BattleEngine(controller, comparator)
    

    # while True:
    #     battler.skill(1,0,0)
    #     battler.skill(2,0,0)
    #     battler.skill(3,0,0)
    #     battler.skill(4,0,0)
